pre-commit:
  parallel: true
  commands:
    eslint:
      glob: '*.{js,ts,jsx,tsx}'
      run: npx eslint {staged_files} --fix
    tsc:
      glob: '*.{ts,tsx}'
      run: npx tsc --noEmit

prepare-commit-msg:
  commands:
    commitlint:
      run: npx commitlint --edit "$1"
